repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: nginx_hf
    namespace: nginx_hf
    chart: bitnami/nginx
    values:
      - image:
          debug: false